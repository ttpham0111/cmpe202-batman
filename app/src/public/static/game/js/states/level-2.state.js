Game.Level2 = function(editor) {
  this._editor = editor;
};

Game.Level2.prototype = {
  init: function() {
    this.physics.startSystem(Phaser.Physics.ARCADE);
  },

  create: function() {
  },

  update: function() {
  },

  run: function() {
  }
};
